#include <iostream>

using namespace std;
int main ()
{
    string phrase = "giraffe";
    phrase[0] = 'A';
    cout<< phrase.substr(6, 0);
    return 0;
};

--find("characters', 0(occured after which entry))
--length()
--substr( number at lenth, number of length)
phrase[] to select any character