#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int wnum = 23;
    double dnum = 78.78587;
    wnum ++;
    cout << wnum;
    return 0;
};

--wnum ++ increment, -- decremetn, then just add any operattor with = like += or -= 
--for tht operation with number like wnum += 70;
-- cout<< pow(76, 89) for x raised to power y, sqrt for square root of a number
--floor for rounding down, ceil for rounding up, round for rounding up
--fmax returns bigger number like fmax(67, 89) 89 will return, like fmin